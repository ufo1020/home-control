doctype
html
head
  meta(charset='UTF-8')
  title Support Panel
  link(rel='import', href='libs/bootstrap/3.3.6/css/bootstrap.min.css')
  script(src='components/webcomponentsjs/webcomponents-lite.js')
  script(src='libs/socket.io/socket.io-1.4.5.js')
  script(src='js/controllers/support-panel.js')
  link(rel='stylesheet', href='css/core.css')
body(ng-controller='mainController as ctrl')
  section.section
    div.top(horizontal center-justified center layout)
        p Select a line in one of the main log windows to get the context around it
    div.middle(layout wrap horizontal around-justified center relative)
        p Select a line in one of the main log windows to get the context around it
    div.bottom
        p Select a line in one of the main log windows to get the context around it
